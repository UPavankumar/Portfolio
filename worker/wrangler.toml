name = "alfred-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# Secrets (set via: npx wrangler secret put GROQ_API_KEY)
# GROQ_API_KEY is stored encrypted in Cloudflare — never in this file
